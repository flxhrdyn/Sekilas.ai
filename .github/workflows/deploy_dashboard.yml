name: Deploy Sekilas.ai Dashboard

on:
  workflow_dispatch:

jobs:
  build-dashboard:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate MAS Backend Logic
        run: python -m compileall backend/rag backend/api backend/services

      - name: Build Docker image (Full Stack MAS)
        run: docker build -t sekilas-ai-dashboard .
